# 牢大音色参考

此文件夹存放用于牢大相关脚本（声音复刻等）的参考音频。

- **`laoda_voice.mp3`**：牢大语音样本，用于 DashScope 声音复刻（`scripts/life-agent/enroll-laoda-voice.mjs`、`create-laoda-2-with-voice.mjs` 会读取）

可将 **`laoda_voice.mp3` 直接提交进仓库**：生产机 `git pull` 后无需再单独上传该文件。若不希望大文件进 Git，也可只在服务器上自备此路径下的 mp3。
