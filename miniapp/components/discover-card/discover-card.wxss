.card-wrap {
  min-width: 0;
}

.cover-wrap {
  position: relative;
  width: 100%;
  padding-bottom: 125%;
  background: #ebe3d4;
  overflow: hidden;
}

.cover {
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
}

.cover-placeholder {
  background: #ebe3d4;
}

.claim-badge {
  position: absolute;
  left: 12rpx;
  top: 12rpx;
  z-index: 2;
  padding: 5rpx 12rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.72);
  border-radius: 6rpx;
  background: rgba(255, 255, 255, 0.86);
  color: #4d463f;
  font-size: 20rpx;
  font-weight: 600;
  line-height: 1.2;
  box-shadow: 0 8rpx 18rpx rgba(26, 23, 20, 0.12);
}

.claim-badge.unclaimed {
  background: rgba(244, 239, 230, 0.9);
  color: #7a5712;
}

.claim-badge.claimed {
  color: #4a5a2f;
}

.pulse-badge {
  position: absolute;
  right: 16rpx;
  top: 16rpx;
  z-index: 2;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.pulse-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: #7a1f1f;
}

.pulse-text {
  background: rgba(26, 23, 20, 0.8);
  color: #f4efe6;
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
}

.mind-badge {
  position: absolute;
  left: 8rpx;
  bottom: 8rpx;
  z-index: 2;
}

.body {
  border-top: 1rpx solid #d8cfbf;
  padding-top: 16rpx;
}

.name-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.name {
  font-size: 30rpx;
  font-weight: 500;
  font-family: "Songti SC", "Noto Serif SC", serif;
  color: #1a1714;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
}

.headline {
  display: block;
  margin-top: 10rpx;
  min-height: 2.6em;
  font-size: 25rpx;
  font-style: italic;
  font-family: "Songti SC", "Noto Serif SC", serif;
  color: #6b6359;
  line-height: 1.35;
  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.questions {
  margin-top: 12rpx;
}

.question {
  display: block;
  font-size: 22rpx;
  color: #8d8478;
  line-height: 1.4;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  padding-left: 8rpx;
}

.meta-row {
  margin-top: 16rpx;
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 12rpx;
}

.meta {
  flex: 1;
  min-width: 0;
  font-size: 22rpx;
  color: #8d8478;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.price {
  flex-shrink: 0;
  font-size: 30rpx;
  font-weight: 500;
  color: #7a1f1f;
}

.price-unit {
  font-size: 20rpx;
  color: #8d8478;
  font-weight: normal;
  font-style: normal;
}
