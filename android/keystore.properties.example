# Copy to keystore.properties and fill in real passwords (do not commit).
# storeFile path is relative to android/app/
storeFile=../brightagent-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=brightagent
keyPassword=YOUR_KEY_PASSWORD
